# tasksmind-ci-test

Throwaway fixture repo for testing TasksMind's GitHub CI failure ingestion (Path 1).

**⚠️ CI is meant to fail on purpose — do not "fix" it.** The failing test on `main` produces a
red GitHub Actions run whose logs exercise the downstream incident normalizer.
